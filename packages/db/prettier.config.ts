import globalConfig from '@ordre/config/prettier';

/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
  ...globalConfig,
};

export default config;
