// TODO: browser logger / error tracking (Sentry) - added when the frontend apps land.
// Kept as an explicit empty stub so `@ordre/monitoring/client` resolves and the
// server/client split exists from day one.
export {};
