# Shared infrastructure env (loaded by every app + package that needs it).
# App-specific secrets live in each app's own .env (e.g. apps/api/.env).

# Database
# Runtime connection - the restricted `ordre_app` role.
DATABASE_URL=
# Migration connection - the table owner. See Setup -> Database Roles & RLS.
DATABASE_OWNER_URL=
