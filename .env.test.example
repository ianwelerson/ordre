# Shared test infrastructure env (loaded when APP_STAGE=test).
# Point this at a DEDICATED, disposable database - the integration test harness
# TRUNCATEs every table on it before each test.

# Database
# Runtime connection - the restricted `ordre_app` role.
DATABASE_URL=
# Migration connection - the table owner. See Setup -> Database Roles & RLS.
DATABASE_OWNER_URL=
